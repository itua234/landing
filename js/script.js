"use strict";

//-->> you can define all, variables, constants, event handlers etc here during testing before 
//-->> moving them to the appropriate file (when the code is stable enough)






var sampleVariable = 'a string';
var SAMPLE_CONSTANT = 'a string';
function sampleFunction() {
    
}